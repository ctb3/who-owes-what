# Copy to .env.local for `npm run dev` against DynamoDB Local.
TABLE_NAME=WhoOwesWhat
DDB_ENDPOINT=http://localhost:8000
AWS_REGION=us-east-1
# Signs event access cookies. Any random string locally; Secrets Manager in AWS.
APP_SECRET=local-dev-secret-not-for-production
